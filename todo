BigGoal:
procedural
>MapGen.make_room(pos1, pos2) => Room
>MapGen.connect_rooms(room1, room2) => Room(connects r1, r2)

Pathfinder.find_path(pos1, pos2) => (Path?) / (Array[dir])

basic entity ai

materials.dat? maybe some walls explode?
>wall.explode_parts {
=> arr, remove elems, use MapGen to connect parts }
=> if !walls @room.delete(self)

WIP: bomb :)
> explosion_effects => 8 entities(Particle < Entity?), dir = compass.dup, dir=straight ? steps = radius : steps = 0.7*radius

Item.weapontxt => Item.weapontxt(max_wid)?
> skaalaa cd baria max_widin mukaan
> jos ei onnistu, lyhennetty weapontxt versio ja uusi yri

- Menu.select_menu
yield action (@weapon / item.sell)

- Menu.shop(items)

-class Path

-module Pathfinder

https://ruby-doc.org/stdlib-2.1.0/libdoc/curses/rdoc/Curses.html